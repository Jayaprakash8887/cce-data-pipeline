<clickhouse>
    <!-- Allow the profile-level tuning settings below to sit at server top level
         (CH 26.3 otherwise errors: "setting 'max_bytes_before_external_group_by' appeared at top level"). -->
    <skip_check_for_incorrect_settings>1</skip_check_for_incorrect_settings>

    <!-- Listen on IPv4 too. On IPv6-disabled hosts the default `::1`-only listener leaves nothing
         on the IPv4 loopback, so healthchecks / clients hitting 127.0.0.1:8123 get "connection refused". -->
    <listen_host>0.0.0.0</listen_host>

    <max_concurrent_queries>100</max_concurrent_queries>
    <max_memory_usage>28000000000</max_memory_usage>
    <max_bytes_before_external_group_by>10000000000</max_bytes_before_external_group_by>

    <merge_tree>
        <max_suspicious_broken_parts>5</max_suspicious_broken_parts>
    </merge_tree>

    <query_log>
        <database>system</database>
        <table>query_log</table>
        <partition_by>toYYYYMM(event_date)</partition_by>
        <flush_interval_milliseconds>7500</flush_interval_milliseconds>
    </query_log>

    <prometheus>
        <endpoint>/metrics</endpoint>
        <port>9363</port>
        <metrics>true</metrics>
        <events>true</events>
        <asynchronous_metrics>true</asynchronous_metrics>
    </prometheus>

    <!-- Kafka-engine ingestion batches via kafka_max_block_size; server-level async inserts with
         wait_for_async_insert=0 risk data loss on crash with no throughput benefit. -->
    <async_insert>0</async_insert>
</clickhouse>
